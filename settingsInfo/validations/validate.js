/**
* Validation code for field shop on settingsInfo
* @param {import("gadget-server").SettingsInfoShopFieldValidationContext } context - All the useful bits for running this validation.
*/
module.exports = async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.shop` to get the fields's data
  // add to the `errors` with `errors.add('shop', "did not pass the validation")`
};